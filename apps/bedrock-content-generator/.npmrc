# Override root ignore-scripts so esbuild's postinstall can run and install the
# platform binary (@esbuild/linux-x64 on CI). Otherwise Vite/vitest fail loading
# vite.config.ts with "The package @esbuild/linux-x64 could not be found".
ignore-scripts=false
