import { css } from 'emotion';
import tokens from '@contentful/f36-tokens';

export const styles = {
  container: css({
    width: '50%',
    marginTop: tokens.spacing2Xl,
    marginLeft: 'auto',
    marginRight: 'auto',
  }),
};
