import { ReviewPayload } from '../utils/types';

/**
 * Paste a `ReviewPayload` JSON object here to jump straight to the Review UI.
 *
 * Keep this file lightweight and local-dev oriented; the entrypoint button is
 * gated behind `import.meta.env.DEV`.
 */
export const mockReviewPayload: ReviewPayload = {};
