import { css } from 'emotion';

export const styles = {
  logo: css(`
  svg {
    height: 16px;
    width: 16px;
  }
`),
  orgLogo: css({
    /** Setting size to default size for favicon in MS organizational branding */
    height: 32,
  }),
};
