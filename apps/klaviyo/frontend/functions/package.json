{
  "name": "klaviyo-entry-sync-function",
  "version": "1.0.0",
  "description": "Automatically sync Contentful entries to Klaviyo on publish/update",
  "main": "dist/entry-sync-function.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prebuild": "rm -rf dist && mkdir -p dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@contentful/node-apps-toolkit": "^2.0.0",
    "contentful-management": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^4.9.0",
    "@types/node": "^18.0.0"
  }
} 