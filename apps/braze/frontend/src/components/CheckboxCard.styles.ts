import tokens from '@contentful/f36-tokens';
import { css } from 'emotion';

export const checkboxCard = css({
  border: `1px solid ${tokens.gray200}`,
  borderRadius: tokens.borderRadiusSmall,
  height: tokens.spacingXl,
});
