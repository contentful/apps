import { css } from '@emotion/react';

export const styles = {
  // How do I access these colors from forma36?
  validCount: css({
    color: '#414D63',
  }),
  invalidCount: css({
    color: '#DA294A',
  }),
};
