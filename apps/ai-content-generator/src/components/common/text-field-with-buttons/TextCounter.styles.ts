import { css } from '@emotion/react';

export const styles = {
  // How do I access these colors from forma36?
  validCount: css({
    marginBottom: 0,
    color: '#414D63',
  }),
  invalidCount: css({
    marginBottom: 0,
    color: '#DA294A',
  }),
};
