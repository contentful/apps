service: google-analytics
frameworkVersion: '3'

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 8080

provider:
  name: aws
  runtime: nodejs18.x

functions:
  api:
    handler: build/index.handler
    events:
      - http:
          path: /{any+}
          method: ANY

