import { css } from 'emotion';
import tokens from '@contentful/f36-tokens';

export const styles = {
  textLink: css({
    lineHeight: tokens.lineHeightS,
  }),
  statusWrapper: css({
    alignItems: 'flex-end',
  }),
};
