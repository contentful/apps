import tokens from '@contentful/f36-tokens';
import { CSSProperties } from 'react';

export const styles = {
  formControlPadding: {
    padding: tokens.spacingS,
    minWidth: '280px',
    maxWidth: '360px',
  } as CSSProperties,
};
