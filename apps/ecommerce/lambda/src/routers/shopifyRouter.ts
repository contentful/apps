import { Router } from 'express';
import { ShopifyController } from '../controllers';
const app = Router();

//app.post('/resource/(product|variant|collection)/:id', ShopifyController.resource);

export default app;
