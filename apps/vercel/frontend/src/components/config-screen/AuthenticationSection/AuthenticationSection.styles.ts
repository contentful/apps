import { css } from '@emotion/css';

export const styles = {
  box: css({
    width: '100%',
  }),
  badgeContainer: css({
    marginTop: '10px',
    width: '100%',
  }),
  heading: css({
    fontSize: '1rem',
  }),
};
