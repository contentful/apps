import tokens from '@contentful/f36-tokens';
import { css } from 'emotion';

export const styles = {
  box: css({
    width: '100%',
  }),
  helpText: css({
    marginBottom: tokens.spacingM,
  }),
};
