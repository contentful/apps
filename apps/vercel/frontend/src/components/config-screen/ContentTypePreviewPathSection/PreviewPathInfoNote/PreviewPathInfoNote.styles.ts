import tokens from '@contentful/f36-tokens';
import { css } from 'emotion';

export const styles = {
  root: css({
    display: 'flex',
    alignItems: 'center',
    marginBottom: tokens.spacingL,
  }),
};
