# Agent Guide — orange-dam-content-browser

## What This App Does
Integrates Orange DAM (Digital Asset Management) with Contentful as an asset browser. Lets editors select assets from Orange DAM for use in Contentful fields.

## Archetype
Unknown — the app directory appears to be empty or has no `package.json` at audit time.

> **Before making any changes**: run `ls apps/orange-dam-content-browser/` to determine the current state of this app. It may be a stub, moved to `marketplace-partner-apps`, or under active development.

## Sharp Edges & Invariants

- Verify the app structure before assuming any specific archetype.
- If a `package.json` is found, identify whether it uses `dam-app-base` or a custom implementation.

## Never / Always

- **Never** assume this app follows the standard Vite archetype without first reading its source.
